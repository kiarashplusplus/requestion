V0:
For /query to return at least one type of results (i.e. `news_article`) for any query.

TODO:
* DONE use News API in results.js
* DONE react-native app to show results
* DONE new overlay endpoint that uploads a sticker to cloudinary and returns a demo image with sticker overlay
* Navigate in app to sticker details view
* carousel for /alternatives of stickerId
* FlatList to SectionList with 2 of the same API calls
* /google

V1:
* extend `template.js` with more types
* group similar sticker types together on app
* add support for multiple MuiTheme in /sticker endpoint
* add sticker page view on react native with sharing buttons and swatch icon for changing themes
