# Direct dependencies for the static-sticker seeding utility
# (cached_query_from_static.py). Transitive deps are resolved by pip; pin a
# lockfile separately if you need reproducible installs.
firebase-admin>=7.0,<8
Pillow>=12.0,<13
